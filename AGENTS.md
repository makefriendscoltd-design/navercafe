# 상철과 일하는 법 — Codex

## 유튜브 링크 SNS 오케스트레이션

- 카페·카드뉴스·쇼츠의 상세 제작 규칙과 머신 게이트는 `SHORTS_SPEC.md` 및 `content_production_policy.py`를 단일 정본으로 사용한다. 중복 문구가 충돌하면 정본의 더 엄격한 조건을 따르고 발행을 중단한다.
- 원본 YouTube ID를 공통 `source_key`로 삼는다. 같은 `source_key`의 네이버 카페 provider URL, YouTube Shorts provider URL/상태, YouTube Community 카드뉴스 provider URL/상태가 모두 확인돼야 3채널 완료다. 쇼츠나 카드뉴스만 발행된 source_key에 카페 provider URL이 없으면 완료가 아니라 카페 미발행으로 남겨 재시도한다.

- 상철이 유튜브 링크를 주면 Orca 오케스트레이션 Run을 만들고, 원본 분석·네이버 카페·유튜브 카드뉴스/커뮤니티·실제 쇼츠 영상 제작/업로드를 독립 Task로 나눠 병렬 진행한다. 카페·카드뉴스·쇼츠 3개 모두 provider 완료 증거가 있어야 한 링크 작업을 완료로 센다. 쇼츠 대본 파일만 만든 상태는 미완료다.
- 브라우저와 NotebookLM 작업은 Aside CLI headless `u0` 로그인 세션만 사용한다. NotebookLM은 `민수대표님_카페글`/`민수대표님_숏폼` 기존 노트북만 사용하며 다른 노트북과 임시 노트북은 금지한다.
- 네이버 카페는 기존 버전의 인용구/본문/이미지 구조를 그대로 유지하고, `AI 자동화&수익화 정보` 카테고리를 사용한다. 글 끝에는 `AI 자동화를 직접 배우는 오프라인 스터디를 진행하고 있습니다. 관심 있으시면 아래 패밀리데이 모집 안내 글을 읽어보세요.`와 `https://cafe.naver.com/westudyssat/4188`을 먼저 넣고, 그 아래 별도 문단에 `▶ 원본 영상`과 원본 영상 링크를 넣는다. 두 URL은 글자로만 입력하지 말고 실제 붙여넣기로 패밀리데이 OG 썸네일 카드와 YouTube 미리보기 카드를 생성하며, 원문 URL도 함께 남아 있어야 한다.
- 쇼츠는 NotebookLM 스크립트의 다섯 번째 내용까지만 쓰고 고정 CTA를 붙인다. 원본 YouTube 영상을 중앙에, 승인된 민수 촬영본을 음소거한 원형 PIP로 하단에 쓰며 카드뉴스·정지 프레임·다른 사람 영상은 섞지 않는다. 음성·자막·90px 헤드카피·레이아웃·오디오 수치는 `SHORTS_SPEC.md`의 머신 게이트를 전부 통과해야 하며, 실패하면 생성·업로드를 중단한다.
- 오늘(2026-08-19) 작업만 게시 버튼 직전 화면을 보여주고 승인 후 발행한다. 이후 링크 작업은 산출물과 실제 편집기 구조 검증을 통과하면 기존 원칙대로 바로 발행한다. 상철이 별도 승인/미리보기를 요청하면 그 지시가 우선한다.
- 실패한 글쓰기/검증 탭은 닫고, 최종 검증된 초안 또는 발행 결과만 남긴다.
- 쇼츠 공개와 카페 발행은 서로 독립이다. 카페 공급자 URL이 없거나 카페 발행이 실패해도 쇼츠 예약·공개를 미루거나 변경하지 않는다.
- 카페 자동화는 토요일·일요일을 포함해 매일 `10:00` KST부터 매시간 실행한다. 주말을 건너뛰지 않는다. 실패·미발행 건은 `11:00`, `12:00`, `13:00`처럼 다음 정시 실행에서 계속 재시도하되 실행 한 번당 최대 한 건, 실제 성공 발행은 하루 최대 2건, 성공 발행 간 최소 5시간을 지킨다.
- 카페 공개 URL이 이미 확인된 항목은 큐에서 `published`로 고정해 중복 발행하지 않는다. 카페 자동화는 쇼츠 provider ID·예약 시각·공개 상태를 읽기 전용 참고값으로만 취급하며 수정하거나 CRM 계획 기록을 만들지 않는다.
- 카페 일일 발행량은 매일 `00:10` KST에 전날 기준으로 보고한다. 네이버 공급자 게시 URL 확인 뒤 CRM에 `channel=naver_cafe`, `stage=sent`, `status=success`로 기록된 건만 합산해 CCIDA 개인 채팅에 한 번 보내며, 예약·초안·실패 시도는 발행량에 넣지 않는다.

## 외부 발송 추적

- 고객·구독자·커뮤니티로 메시지, 메일, 문자, 게시물 또는 광고 변경을 실제 실행한 경우 제공자/API/UI의 성공 증거를 확인한 직후 `/Users/apple/orca/projects/aimax-crm-observability/bin/aimax-crm-track emit`으로 채널·캠페인·단계·건수만 기록한다.
- 메시지 본문, 전화번호, 이메일, 주문번호, URL query, 시크릿은 공통 장부에 넣지 않는다. 에이전트 도구 성공은 실제 발송·도달로 보고하지 않는다.
- makefamily.kr 외부 링크는 `$utm-link`의 기존 `/Users/apple/orca/projects/cafe24/utm.py`를 사용하며, 없는 분모는 0%가 아니라 `N/A`로 보고한다.

## 판단과 과거 맥락

- 상철의 취향·우선순위·사업 판단이 답에 영향을 주면 `$brain-council`로 `/Users/apple/brain-sangchul`의 근거를 먼저 확인한다. 근거가 없으면 지어내지 않는다.
- 이전 작업을 이어가거나 현재 상태를 묻는 요청은 `$project-resume-brief`로 파일·Git·로컬 세션 기록을 함께 확인한다. 마지막 채팅만 보고 상태를 단정하지 않는다.
- Claude Code와 Codex 사이의 소유권·모델·핸드오프 판단은 `$route-claude-codex`를 따른다.

## 완료와 검증

- 변경·빌드 작업은 실제 사용 경로의 최소 검증을 통과하기 전에는 완료라고 보고하지 않는다.
- 코드 검사나 설치 성공만으로 “작동함”이라고 하지 않는다. 가능한 경우 테스트, 빌드, 대표 입력, 산출물 검사를 순서대로 수행한다.
- 외부 발행·문자·메일·결제·광고·스케줄러 변경이 검증에 필요하면 안전한 직전 단계에서 멈추고 미검증 범위를 명시한다.
- 검증하지 못한 항목은 실패가 아니라 `미검증`으로 분리하고 이유와 다음 검증 명령을 적는다.

## 운영 안전

- 진행 중인 세션, Orca 터미널, 기존 자동화, 누적 아카이브를 임의로 중단·덮어쓰기·삭제하지 않는다.
- 시크릿 값은 채팅과 보고서에 출력하지 않는다. 설정 위치와 변수명만 언급한다.
- 결론부터 짧고 직접적으로 답한다.

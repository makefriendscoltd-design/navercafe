# 네이버 카페 자동 댓글봇 — PRD & 생초보 따라하기 가이드

> 이 문서 하나만 보고 컴퓨터를 처음 만지는 분도 네이버 카페 "가입인사 자동 댓글봇"을
> 직접 깔고, 24시간 자동으로 돌릴 수 있게 만든 안내서입니다.
> 위쪽은 "이게 뭐 하는 물건인지(PRD)", 아래쪽은 "그래서 어떻게 까는지(따라하기)"입니다.

---

## 1. 한 줄 요약

**네이버 카페 가입인사 게시판에 새 글이 올라오면, 미리 정해둔 환영 문구 중 하나를 자동으로 댓글로 달아주는 프로그램입니다.** (덤으로 매일/매주 카페 통계를 텔레그램으로 보고해 줍니다.)

---

## 2. 왜 만들었나 (배경)

- 카페에 가입인사가 하루에도 여러 개씩 올라오는데, 운영진이 일일이 환영 댓글을 다는 건 시간 낭비입니다.
- 그렇다고 안 달면 신규 회원이 "여기 죽은 카페인가?" 하고 떠납니다.
- 환영 댓글에 **무료 라이브 / 유료강의 / 오프라인 스터디 / 추천 게시판 링크**를 자연스럽게 넣으면, 신규 회원을 곧바로 활동·전환으로 유도할 수 있습니다.
- 즉, 이 봇은 단순 자동화가 아니라 **"신규 회원 온보딩 + 마케팅 유입" 자동화 도구**입니다.

---

## 3. 누가 쓰면 좋은가 (대상 사용자)

| 이런 분 | 이렇게 도움 됩니다 |
|---|---|
| 네이버 카페 운영자 | 가입인사 환영 댓글을 24시간 자동으로 |
| 1인 / 소규모 팀 | 사람 없이도 새벽·주말 가입자까지 케어 |
| 마케터 | 환영 댓글에 유입 링크를 심어 전환 유도 |
| 컴맹 초보 | 이 문서대로 복붙만 하면 됩니다 (코딩 지식 0 OK) |

---

## 4. 핵심 기능 (무엇을 하나)

1. **새 글 자동 감지** — 지정한 게시판(가입인사)을 정해진 주기(기본 30분)마다 확인합니다.
2. **랜덤 환영 댓글** — 미리 등록한 여러 문구 중 하나를 무작위로 골라 답니다. (매번 똑같은 댓글 → 스팸처럼 보이는 것 방지)
3. **봇 시작 이후 글만** — 켠 시점의 기존 글에는 안 달고, 그 뒤로 새로 올라온 글에만 답니다. (과거 글에 한꺼번에 달고 싶으면 "백필" 기능 사용)
4. **중복 방지** — 한 번 댓글 단 글은 기억해서(`comment_bot_seen.json`) 다시 켜도 또 달지 않습니다.
5. **공지글 제외** — 공지/필독글에는 댓글을 달지 않습니다.
6. **24시간 자가 복구** — 봇이 죽거나 컴퓨터가 절전됐다 깨어나도, 감시자(작업 스케줄러)가 5분 안에 다시 살려냅니다.
7. **텔레그램 보고** *(선택)* — 매일 아침 "어제 가입인사 N건 / 댓글 성공·실패 / 방문자·조회수 TOP3"를, 매주 월요일엔 "지난주 신규 가입 회원수"를 텔레그램으로 보내줍니다.

---

## 5. 어떻게 작동하나 (원리, 그림으로)

```
[30분마다]
  카페 가입인사 게시판 열기
        │
        ▼
  새 글 있나? ──아니오──▶ 30분 대기 후 다시 확인
        │ 예
        ▼
  공지글인가? ──예──▶ 건너뜀
        │ 아니오
        ▼
  환영 문구 4개 중 1개 랜덤 선택
        │
        ▼
  댓글창에 붙여넣기 → "등록" 클릭
        │
        ▼
  "이 글은 처리함" 기록 (중복 방지)
```

- 실제 크롬 브라우저를 띄워서 사람이 하듯 클릭/붙여넣기 합니다. (네이버 봇 감지 회피를 위해 화면에 크롬 창이 보이게 동작합니다 — 정상입니다.)
- 한 번 로그인하면 전용 크롬 프로필에 세션이 저장돼, 이후엔 매번 로그인/캡차를 안 거칩니다.

---

## 6. 미리 알아둘 한계·주의사항 ⚠️

- **Windows 전용**입니다. (Mac/리눅스 ❌)
- **컴퓨터가 켜져 있어야** 돌아갑니다. 24시간 돌리려면 안 끄는 PC가 필요합니다. (절전 모드는 깨어나면 자동 복구되지만, 완전 종료되면 안 됩니다.)
- **네이버 화면 로그인 세션이 필요**합니다. 봇이 크롬 창을 띄우므로, 그 화면이 살아있어야 합니다. (원격데스크톱으로 끊으면 화면이 사라져 멈출 수 있음 → 끊을 땐 콘솔 세션 유지 옵션 권장)
- **과도하게 쓰면 네이버에서 제재**될 수 있습니다. 댓글 주기를 너무 짧게(예: 10초)로 하지 마세요. 기본값(30분)을 권장합니다.
- **환영 댓글 문구는 자연스럽게.** 광고티가 너무 나면 회원이 거부감을 느낍니다.

---
---

# 🛠️ 여기서부터 생초보 따라하기 (복붙만 하면 됩니다)

> 💡 **순서대로 한 칸씩** 따라오세요. 건너뛰면 안 됩니다.
> 막히면 "8. 자주 막히는 곳" 표를 보세요.

## STEP 0. 준비물 체크 ✅

- [ ] Windows 컴퓨터 (가급적 끄지 않을 PC)
- [ ] 댓글을 달 **네이버 아이디/비밀번호** (카페에 댓글 쓸 수 있는 등급이어야 함)
- [ ] 댓글봇을 돌릴 **네이버 카페**와 **가입인사 게시판**
- [ ] (선택) 통계 보고를 받을 텔레그램

---

## STEP 1. 프로그램(파이썬) 설치

1. 인터넷 주소창에 `python.org/downloads` 입력 → 접속
2. 노란 버튼 **"Download Python 3.12.x"** 클릭 → 다운로드된 파일 실행
3. ⚠️ **가장 중요**: 설치 첫 화면 맨 아래 **`Add python.exe to PATH`** 체크박스를 **반드시 체크** → 그 다음 **Install Now** 클릭
4. 설치 끝나면 닫기

> 체크를 안 하고 설치했다면 → 파이썬을 지우고 처음부터 다시, 이번엔 꼭 체크하세요.

**크롬 브라우저**도 깔려 있어야 합니다. (없으면 `google.com/chrome` 에서 설치)

---

## STEP 2. 봇 파일 받기 & 폴더 만들기

1. `C:` 드라이브 같은 곳에 폴더 하나 만들기. 예) `C:\commentbot`
2. 운영자에게 받은 아래 파일을 그 폴더에 넣기:
   - `comment_bot.py` ← **핵심 프로그램 (필수)**
   - `watchdog_comment_bot.ps1` ← 24시간 자동 재시작용 (나중에 STEP 7에서 사용)
3. 같은 폴더에 **메모장으로 `config.ini` 파일을 새로 만들기** (다음 STEP에서 내용 채움)

> 📁 이 가이드에서는 폴더를 `C:\commentbot` 으로 가정합니다. 다른 곳에 두면, 뒤에 나오는 경로(`C:\commentbot`)를 본인 폴더로 바꿔서 읽으세요.

---

## STEP 3. 필요한 부품(패키지) 설치

1. 윈도우 검색창에 `cmd` 입력 → **명령 프롬프트** 실행
2. 아래를 그대로 복사해 붙여넣고 Enter:

```
pip install selenium pyperclip
```

3. 막 글자가 올라가다가 멈추면 성공입니다. (`Successfully installed ...` 가 보이면 OK)

> "pip는 내부 또는 외부 명령... 아닙니다" 오류가 나면 → STEP 1의 PATH 체크를 안 한 것입니다. 파이썬 재설치하세요.

---

## STEP 4. 설정 파일 `config.ini` 채우기 ⭐ (가장 중요)

STEP 2에서 만든 `config.ini`를 메모장으로 열고, **아래를 통째로 복사해 붙여넣은 뒤** `여기에_...` 부분만 본인 것으로 바꾸세요.

```ini
[NAVER]
id = 여기에_네이버_아이디
pw = 여기에_네이버_비밀번호

[COMMENT_BOT]
board_url = 여기에_가입인사_게시판_주소
poll_interval_sec = 1800
min_action_delay = 3
max_action_delay = 8

comment_1 = 가입을 진심으로 환영합니다! 앞으로 자주 뵈어요 :)
comment_2 = 반갑습니다~ 좋은 정보 많이 나누면서 함께해요!
comment_3 = 환영합니다! 궁금한 점 있으면 편하게 물어보세요 :)
comment_4 = 어서오세요~ 활동 기대하겠습니다!
```

### 각 항목 설명

| 항목 | 뜻 | 예시 |
|---|---|---|
| `id` / `pw` | 댓글 달 네이버 계정 | `myid` / `mypw123!` |
| `board_url` | 감시할 가입인사 게시판 주소 (STEP 5 참고) | 아래 참고 |
| `poll_interval_sec` | 몇 초마다 새 글 확인할지 | `1800` = 30분 (권장) |
| `min/max_action_delay` | 댓글 달기 전 랜덤 대기(초). 봇 티 안 나게 | 그대로 두세요 |
| `comment_1, 2, 3...` | 랜덤으로 달릴 환영 문구들 | 원하는 만큼 추가 |

### 환영 문구(comment_N) 작성 팁

- **여러 개 만들수록 좋습니다** (`comment_5`, `comment_6` ... 번호만 늘리면 됨). 그래야 매번 다른 댓글이 달려 자연스럽습니다.
- **줄바꿈을 넣고 싶으면** 글자 사이에 `\n` 을 쓰세요. (엔터 말고 백슬래시+n)
  예: `반갑습니다!\n\n아래 링크를 확인하세요 → https://...`
- **링크를 넣어도 됩니다.** 환영 + 유입을 동시에:
  ```
  comment_2 = 반갑습니다! 무료 라이브는 매주 화·목 진행해요. 아래에서 신청하세요!\n\nhttps://litt.ly/내링크
  ```

> ⚠️ **저장할 때**: 메모장 → `파일 > 다른 이름으로 저장` → 파일 이름 `config.ini`,
> **인코딩을 반드시 `UTF-8`** 로 선택하세요. (한글이 깨지지 않게)
> 파일이 `config.ini.txt` 로 저장되지 않게 주의 — 이름 끝이 `.ini` 여야 합니다.

---

## STEP 5. 가입인사 게시판 주소(board_url) 알아내기

1. 크롬에서 본인 카페의 **가입인사 게시판**으로 들어갑니다.
2. 주소창의 URL을 **통째로 복사**합니다. 보통 이렇게 생겼습니다:
   ```
   https://cafe.naver.com/f-e/cafes/26321967/menus/105
   ```
   - `cafes/` 뒤 숫자(`26321967`) = 카페 번호
   - `menus/` 뒤 숫자(`105`) = 게시판 번호
3. 이 주소를 `config.ini`의 `board_url =` 뒤에 붙여넣습니다.

> 주소에 `?viewType=L` 같은 게 붙어 있어도 그냥 둬도 됩니다.

---

## STEP 6. 첫 실행 & 테스트 (진짜 잘 되나 확인)

### 6-1. 셀렉터/로그인 점검 (댓글은 안 닮)

명령 프롬프트에서:

```
cd C:\commentbot
python comment_bot.py inspect
```

- 크롬 창이 뜨고 네이버 로그인 화면이 나오면, **처음엔 직접 로그인**하세요. (캡차/2차 인증이 나오면 손으로 통과 — 이때 한 번만 하면 다음부턴 자동입니다.)
- 화면에 `입력창 발견`, `등록버튼 발견` 비슷한 메시지가 보이면 정상입니다.

### 6-2. 실제 테스트 댓글 1개 달아보기

```
python comment_bot.py test
```

- 게시판 맨 위 글에 **테스트 댓글 1개를 실제로** 답니다. 카페에서 잘 달렸는지 눈으로 확인하세요. (확인 후 그 댓글은 직접 지우면 됩니다.)

### 6-3. 본격 가동 (상시 감시)

```
python comment_bot.py
```

- "기준선 기록" 메시지가 나오고, 이후 **새로 올라오는 가입인사부터** 자동으로 댓글을 답니다.
- 끄려면 그 창에서 `Ctrl + C`.

> 이 상태(검은 창 켜둔 상태)로도 동작은 합니다. 다만 컴퓨터를 껐다 켜면 다시 명령어를 쳐야 하죠. 그걸 자동화하려면 STEP 7로.

---

## STEP 7. 24시간 자동으로 돌리기 (작업 스케줄러 등록)

> 컴퓨터를 켜두기만 하면, 봇이 꺼져도 **5분마다 자동으로 다시 켜주는** 감시자를 등록합니다.

1. 윈도우 검색창에 `작업 스케줄러` 입력 → 실행
2. 오른쪽 **"작업 만들기"**(기본 작업 말고 "작업 만들기") 클릭
3. **[일반] 탭**
   - 이름: `CommentBot` (아무거나)
   - **"사용자가 로그온할 때만 실행"** 선택 (봇 크롬 창이 보여야 하므로)
   - "가장 높은 권한으로 실행" 체크
4. **[트리거] 탭** → 새로 만들기
   - "작업 시작": **반복 작업 예약**
   - 매일 → "다음 시간마다 작업 반복": **5분**, 기간: **무기한**
   - 확인
5. **[동작] 탭** → 새로 만들기
   - 작업: **프로그램 시작**
   - 프로그램/스크립트: `powershell.exe`
   - 인수 추가:
     ```
     -NoProfile -ExecutionPolicy Bypass -WindowStyle Hidden -File "C:\commentbot\watchdog_comment_bot.ps1"
     ```
   - ⚠️ `watchdog_comment_bot.ps1` 안에도 폴더 경로가 적혀 있습니다. 메모장으로 열어 `D:\coding\ccidacafe` 라고 된 곳을 본인 폴더(`C:\commentbot`)로 바꾸세요. (2군데: `$dir = ...`)
6. **[설정] 탭** → "작업이 이미 실행 중이면..." → **"새 인스턴스를 시작하지 않음"** 선택
7. 확인 → 비밀번호 물어보면 윈도우 로그인 암호 입력

이제 5분마다 감시자가 "봇 살아있나?" 확인하고, 죽어 있으면 자동으로 켜줍니다. ✅

---

## STEP 8. 자주 막히는 곳 (트러블슈팅)

| 증상 | 원인 / 해결 |
|---|---|
| `pip ... 명령이 아닙니다` | STEP 1에서 PATH 체크 안 함 → 파이썬 재설치 |
| 한글 댓글이 깨져요 | `config.ini`를 **UTF-8**로 저장 안 함 → 다시 저장 |
| `config.ini 가 없습니다` | 파일 이름이 `config.ini.txt`로 저장됨 → 확장자 `.txt` 떼기 |
| 로그인 화면에서 멈춤 | 캡차/2차 인증 → 첫 1회는 직접 통과 (이후 자동) |
| `Chrome instance exited` | 좀비 크롬이 프로필 잠금 → 봇은 자동 정리하지만, 안 되면 작업관리자에서 chrome.exe 종료 후 재시작 |
| 댓글이 안 달려요 | 게시판 주소(board_url)가 가입인사 게시판이 맞는지 확인 |
| 같은 글에 또 달려요 | 거의 없음. `comment_bot_seen.json` 파일이 처리 기록 — 지우면 다시 달 수 있으니 함부로 지우지 마세요 |

---

## STEP 9. (선택) 텔레그램 보고 받기

매일 아침 "어제 가입 N건 / 댓글 성공·실패 / 방문자·조회수 TOP3", 매주 월요일 "지난주 신규 가입 회원수"를 받고 싶다면:

1. 텔레그램에서 `@BotFather` 검색 → `/newbot` → 봇 만들고 **토큰** 받기
2. 그 봇을 보고 받을 채팅방/그룹에 초대
3. 채팅방 **chat_id** 알아내기 (그룹이면 보통 `-100...`으로 시작. 검색: "텔레그램 chat id 확인 방법")
4. `config.ini`의 `[COMMENT_BOT]` 아래에 추가:

```ini
telegram_enabled = true
telegram_token = 여기에_봇파더가_준_토큰
telegram_chat_id = 여기에_chat_id
telegram_report_hour = 9
telegram_weekly_enabled = true
telegram_weekly_report_day = 0
```

| 항목 | 뜻 |
|---|---|
| `telegram_report_hour` | 매일 보고 시각 (9 = 오전 9시) |
| `telegram_weekly_report_day` | 주간 보고 요일 (0=월, 6=일) |

---

## STEP 10. (선택) 과거 글에 한꺼번에 달기 — 백필

봇을 켜기 전에 올라온 옛날 가입인사들에도 소급해서 달고 싶다면:

```
:: 먼저 미리보기 (실제로 안 달림, 대상만 보여줌)
python comment_bot.py backfill 2026-06-01

:: 진짜로 달기
python comment_bot.py backfill 2026-06-01 go
```

- 날짜(`2026-06-01`)는 "이 날짜 이후 글에 달아라"는 뜻입니다.
- ⚠️ 백필 전에는 **상시 감시봇(작업 스케줄러)을 잠깐 멈추세요.** 둘이 같이 크롬을 쓰면 충돌합니다.
- 백필은 한꺼번에 많이 다는 작업이라, 스팸 감지 회피를 위해 글당 15~40초씩 천천히 답니다.

---

## 부록 A. 명령어 모음 (치트시트)

| 하고 싶은 것 | 명령어 |
|---|---|
| 점검 (댓글 안 닮) | `python comment_bot.py inspect` |
| 테스트 댓글 1개 | `python comment_bot.py test` |
| 본격 가동 | `python comment_bot.py` |
| 통계 지금 보내기 | `python comment_bot.py report` |
| 과거 글 미리보기 | `python comment_bot.py backfill 날짜` |
| 과거 글 실제 작성 | `python comment_bot.py backfill 날짜 go` |
| 중지 | 실행 창에서 `Ctrl + C` |

## 부록 B. 폴더에 생기는 파일들 (지우지 마세요)

| 파일 | 역할 |
|---|---|
| `config.ini` | 내 설정 (계정·게시판·문구) |
| `comment_bot_seen.json` | 이미 댓글 단 글 기록 (중복 방지) |
| `comment_bot_weekly.json` | 주간 보고 중복 방지 기록 |
| `comment_bot.log` | 실행 로그 (문제 생기면 여기 확인) |
| `chrome_profile_commentbot/` | 로그인 세션 저장 폴더 (지우면 재로그인) |
| `comment_bot_watch.lock` | 봇 중복 실행 방지용 잠금 파일 |

---

> 📌 **보안 주의**: `config.ini`에는 네이버 비밀번호·텔레그램 토큰이 들어갑니다.
> 이 파일은 **절대 외부에 공유하지 마세요.** 남에게 줄 땐 `comment_bot.py`와 `watchdog_comment_bot.ps1`만 주고, `config.ini`는 각자 본인 것으로 만들게 하세요.

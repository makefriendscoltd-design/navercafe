# 나민수 AI 3채널 제작 정본

이 문서와 `content_production_policy.py`가 유튜브 링크 재가공의 정본이다. 문서와 코드가 다르면 더 엄격한 조건을 적용하고 발행을 중단한다.

## NotebookLM

- 브라우저는 Aside CLI headless, 계정 별칭은 `u0`만 쓴다. Chrome 쿠키, Selenium, Playwright, `notebooklm-py`, 새 임시 노트북은 쓰지 않는다.
- 카페 원고는 기존 `민수대표님_카페글`(`c09a56d4-b87c-4f54-bfdb-93219326fbae`)만 쓴다.
- 쇼츠 대본은 기존 `민수대표님_숏폼`(`ed70fc3b-474b-423a-9ca8-d19934703f27`)만 쓴다.
- 쇼츠 노트북 맞춤 지침은 `content_production_policy.py`의 v16.0 정본만 쓴다. 정규화 SHA-256은 `f08aa788417fdc1cd7958dc0f8530c8482128f0d0c5625b103ae6a66a5862146`이며, Aside는 소스를 추가하기 전에 로컬 v16 금지·문단 형식 계약, 지침 해시, `맞춤`/`길게` 상태를 확인한다. 하나라도 다르면 소스 추가·프롬프트 제출을 중단한다.
- `그지마케팅_카페글`, `그지마케팅_숏폼스크립트` 등 다른 노트북 응답은 폐기한다.
- 대상 YouTube 소스 하나만 체크된 DOM을 제출 직전과 응답 후 확인하고, 노트북 제목·ID·선택 소스·프롬프트·응답·스크린샷을 증거로 남긴다.
- 영상에 없는 숫자·성과는 만들지 않는다. 수익·성과는 `영상 제작자 주장`으로 표시하고, 변동 가능하거나 중요한 주장은 타임스탬프와 공식 1차 자료로 교차 확인한다.

## 카페 글

- `AI 자동화&수익화 정보` 카테고리를 사용한다.
- 인용구는 소제목에만 적용한다. 본문 전체가 인용구로 들어가면 발행하지 않는다.
- 기존 승인본의 인용구/본문/이미지 순서를 유지하고, 발행 전 전체 DOM과 스크린샷을 확인한다.
- 끝에는 고정 오프라인 스터디 문구와 패밀리데이 링크/OG 카드를 먼저 넣고, 별도 문단에 `▶ 원본 영상`과 원본 URL/YouTube 미리보기 카드를 둔다.

## 카드뉴스

- 정확히 10장, 각 장은 `1080×1080` 정사각형으로 만든다. 4:5 이미지는 사용하지 않는다.
- 기존 승인 레퍼런스의 여백·타이포·색상 계층을 유지하고, 모든 텍스트가 안전 영역 안에 있는지 접촉 시트로 확인한다.
- 카드뉴스는 `표지 1장 → 원본의 방법·과정·도구·실패 지점 8장 → 고정 CTA 1장`으로 구성한다. 표지는 독자가 얻을 내용이나 궁금증으로 시작하며 면책·검증 문구로 시작하지 않는다.
- `verified-facts.json`은 과장을 막는 가드레일로만 쓴다. `content_wording`, `제작자 주장`, `독립 검증`, `단정하면 안 됨` 같은 팩트팩 판정 문구를 카드 본문처럼 나열하지 않는다.
- Community 예약 목록이 긴 원본 URL을 `...`로 줄여 표시하면 화면 문자열만으로 실패·성공을 판정하지 않는다. 같은 `contentText` run의 provider-native navigation endpoint로 정확한 원본 URL을 복원하고, 복원한 전체 본문이 정본과 일치하며 endpoint도 정확한 경우에만 provider 검증을 통과한다. endpoint가 없거나 다르면 재게시하지 않고 실패로 남긴다.
- 영상 속 경험·수치는 필요한 카드 한 곳에서 출처를 자연스럽게 밝힌다. 주의·한계가 카드의 주제가 되는 것은 최대 2장이고, 6장 이상은 독자가 바로 이해하거나 써먹을 정보여야 한다.
- 마감 CTA는 `댓글 AIMAX`와 `관련 정보 받기`로 고정한다. 렌더 전에 `validate_cardnews_editorial_deck`을 통과하지 못하면 생성·예약을 중단한다.
- 카드뉴스 이미지를 쇼츠 화면 소스로 섞지 않는다. 쇼츠 중앙 화면은 원본 YouTube 영상만 쓴다.

## 쇼츠 대본과 헤드카피

- NotebookLM 응답의 `### 스크립트` 아래에서 다섯 번째 항목까지만 사용한다. 6번째 이후와 노트북 CTA는 제거하고 아래 고정 CTA를 붙인다.
- NotebookLM에는 `이 영상으로 숏폼 스크립트 만들어줘.`만 요청한다. 별도 메타프롬프트로 내용을 재구성하지 않는다.
- 같은 지침 버전에서 사실 게이트에 실패한 실질적으로 같은 응답을 다시 뽑지 않는다. 맞춤 지침 버전이 바뀌고 새 사전검증을 통과한 경우에만 해당 소스를 정확히 한 번 다시 시도한다.
- source_key별 시도 ledger에 지침 버전·해시와 `started` 상태를 공급자 동작 전에 원자적으로 남긴다. 같은 버전·해시의 시도 시작, 응답 수신, 실질적 실패, 성공 중 하나라도 기존 ledger나 provider 증거에서 확인되면 소스 추가 전에 재추출을 중단한다. 종료 직전 장애로 응답 제출 여부를 알 수 없는 시도도 같은 해시에서는 재시도하지 않는다.
- 도입과 첫째~다섯째 본문은 글자 그대로 보존한다. 사실확인·팩트팩·윤문 단계가 `제작자 주장`, `독립 검증`, `보장되지 않음`, `확인 필요` 같은 문장을 추가하거나 본문을 다시 쓰면 발행하지 않는다.
- 사실 검증이 필요한 항목은 별도 근거 문서에서 확인하되, 그 결과로 쇼츠 원고를 재작성하지 않는다. 중대한 오류 때문에 원고를 그대로 쓸 수 없으면 문구를 보강하지 말고 해당 영상 제작을 중단한다.
- 최종 원고 변환 증거에는 `notebooklm_body_preserved_exactly=true`, `content_rewrite_applied=false`, CTA 교체 전후 본문 SHA-256 일치를 남긴다.
- 보존 증거는 provider `innerText` 원응답 → 각주 마커/말미 출처 목록 정리 → 스크립트 헤딩 이후 파싱 및 반복 빈 줄·기호 단독 줄 정규화 → 6번째 이후/기존 CTA suffix 절단 → 고정 CTA 추가를 서로 다른 단계로 기록한다. provider 원응답, 각주 정리 응답, parser 본문, 실제 채택 본문, 최종 원고 안의 채택 본문 해시를 모두 남긴다. `notebooklm_body_preserved_exactly` 값은 실제 채택 본문이 고정 CTA 앞에 바이트 단위로 같을 때만 true다.
- 실제 채택 본문은 도입을 포함하고 `첫째`~`다섯째`가 각각 정확히 한 번씩 순서대로 있어야 한다. 6번째 이후 항목, 타임스탬프, `[훅]`·`[본문]`·`[결론]`·`[CTA]` 같은 메타 레이블이 남으면 생성·업로드를 중단한다.
- NotebookLM provider `innerText`의 `### 스크립트` 아래는 도입, 첫째, 둘째, 셋째, 넷째, 다섯째의 정확히 6개 논리 문단이어야 한다. 정본 지침은 문단 사이 빈 줄 하나를 요구한다. 다만 provider UI가 빈 줄만 제거해도 여섯 부분이 정확히 여섯 개의 비어 있지 않은 한 줄 블록으로 남고 `첫째,`부터 `다섯째,`까지가 각 블록의 첫 글자로 정확히 시작하면, 구분자만 결정적으로 빈 줄 문단으로 정규화한다. 내용·문장·순서는 바꾸지 않는다. 여섯 부분을 한 줄에 이어 쓴 평탄화 응답, 추가 줄·문단, 잘못된 순서, `다섯째` 뒤 버전 표기·지침 요약·주석·메타데이터·다른 문장은 생성·업로드를 중단한다.
- 첫째~다섯째의 제목과 핵심 행동은 원본에서 확인한 다섯 지점을 실제 순서대로 각각 이어받아야 한다. 출처의 구체적 행동을 일반적인 이름으로 바꾸거나 다른 항목으로 대체하지 않는다. 전역 정본은 이 계약과 순서 구조를 강제하고, source_key별 adapter의 `validate_scene_sentinels`가 실제 원본별 핵심어를 fail-closed로 검사한다.
- 동일 instruction version의 NotebookLM 시도가 내용 게이트에서 terminal 실패한 뒤에는 provider를 다시 호출하지 않는다. 원응답·실패 단계·시도 장부를 그대로 보존하고, 해당 source_key의 `verified-facts.json`에서 `status=verified`인 `content_wording`만 사용해 별도 복구 원고를 만들 수 있다. 이 경로는 첫째~다섯째 원본 순서, 공통 금지 주장, source_key별 사실·장면 게이트, 헤드카피 90px 게이트를 모두 다시 통과해야 하며 `recovery_mode=verified_factpack_rebuild`, `provider_retry_performed=false`, `notebooklm_body_preserved_exactly=false`, `content_rewrite_applied=true`, 원응답·복구 원고·팩트팩 SHA-256을 증거에 남긴다. 검증되지 않은 새 주장이나 임의 원고는 허용하지 않는다.
- CTA·콜투액션·행동 유도처럼 시청자 행동 지시를 설명하면 `CTA 반영은 영상 제작자의 시연 사례입니다.`와 `결과는 보장되지 않습니다.`를 각각 정확히 한 번 글자 그대로 쓴다. 이 경계가 없거나 CTA 반영을 일반 기능처럼 단정하면 생성·업로드를 중단한다.

```text
OO분 짜리 영상 내용을 모두 정리했습니다.

이 자료 궁금하신 분들은 채널을 구독후 프로필 링크를 확인하세요.
```

- 첫 문장은 `이 남자 미쳤습니다.`, `이 프로그램 대박입니다.` 같은 짧고 강한 후킹으로 시작한다.
- 2줄 헤드카피 후보 3개를 만들고 1안을 사용한다. 각 줄 18자 이하이며 첫 대사가 같은 약속을 바로 이어받는다.
- 헤드카피는 1080×1920 기준 90px, 중앙 x=540, y=440, 줄 간격 20으로 고정한다.
- 헤드카피 3안 모두 정본 BM HANNA 11yrs old 폰트 90px 실측 폭이 줄마다 920px 이하여야 한다. 글자 수가 18자 이하여도 이 폭을 넘으면 libass에서 3줄로 접힐 수 있으므로 렌더 전에 실패 처리한다.
- NotebookLM 사전 생성 지침은 한글 표시 문자 기준 공백 포함 13자 이하를 안전 프록시로 쓴다. 프록시를 통과해도 90px/920px 실측 게이트가 최종 판정이다.
- NotebookLM 그대로 보존 본문에서 지원 범위를 모든/어떤 소스로 일반화하거나, 2클릭 완성을 보장하거나, 무료·고정 생성 시간·자동 다중 플랫폼 배포를 단정하면 제작을 중단한다. 이 공통 금지 게이트는 소스별 팩트팩 게이트에 더해서 적용하며 대체하지 않는다.
- Repurpose 또는 플랫폼 배포를 언급한다면 `Repurpose는 NotebookLM과 별개의 외부 워크플로우입니다.`와 `별도 연결 설정과 각 플랫폼 공급자 지원이 확인된 채널에만 배포할 수 있습니다.`만 글자 그대로 써야 한다. 이 두 문장 외의 Repurpose·플랫폼 배포 문장은 긍정·부정·제작자 귀속·조건·이중부정 여부와 무관하게 모두 금지한다. 앞 문장의 Repurpose를 `이 도구`, `이 앱`, `이 서비스`, `이 프로그램`으로 바꿔 이어 쓰는 문장도 같은 금지 대상으로 본다.
- 원본 분석의 다섯 지점 중 Repurpose 또는 플랫폼 배포가 포함되면 해당 지점을 생략하거나 다운로드만으로 대체하지 않는다. 대응 항목에 위의 정확한 두 경계 문장을 쓰며, source_key별 adapter가 원본 순서와 포함 여부를 별도로 검사한다.
- 업로드 제목 끝에는 `#Shorts`를 포함한 해시태그를 붙이지 않는다.

## 영상과 민수 PIP

- 출력은 1080×1920, 30fps다.
- 중앙은 원본 YouTube 영상만 사용한다: x=0, y=664, 1080×608, 2배속. 출처 파일과 SHA-256을 기록한다.
- 하단 원형 PIP는 x=325, y=1298, 430×430이다. `/Users/apple/Downloads`의 다음 민수 촬영본 4개 중 해시가 맞는 파일만 쓴다.
  - `2026-07-02 15-39-18.mp4`
  - `2026-06-29 16-32-49.mp4`
  - `2026-07-16 18-23-45.mp4`
  - `2026-07-16 18-21-06.mp4`
- 원본 영상과 민수 촬영본의 오디오는 제거한다. 카드뉴스·정지 프레임·다른 사람 영상·다른 사람 얼굴로 대체하지 않는다.
- `@aimax`는 x=540, y=1768, 44px로 유지한다.
- 예전 프레임 슬라이드 렌더러는 폐기했다. `shorts_video.render_short_video()`는 잘못된 영상 생성을 막기 위해 fail-closed로 중단한다.

## 음성과 자막

- ElevenLabs Voice ID `34bevfaPHev7LXnjGAlA`, 모델 `eleven_multilingual_v2`만 쓴다.
- 음성 설정은 stability 0.65, similarity_boost 0.90, style 0, speaker boost 켜짐으로 고정한다. 다른 음성이나 시스템 TTS로 폴백하지 않는다.
- 긴 대본을 한 번에 합성하지 않는다. 도입, 첫째~다섯째, 고정 CTA의 정확히 7구간을 같은 Voice ID·모델·설정으로 각각 합성한 뒤 순서대로 결합한다. 구간 경계에는 0.24초 간격을 두고, 결합본에 silence -35dB, minimum 0.08초, retained gap 0.06초의 꼬리물기를 적용한다.
- 최종 자막 정렬값으로 초반·중반·후반의 실제 발화 문자/초를 계산한다. `후반 문자/초 ÷ 초반 문자/초`가 1.10을 넘으면 후반 가속 실패로 보고 생성·업로드를 중단한다.
- 자막은 발음 정렬값에 맞춘 한 어절/토큰 단위다. 이웃 어절을 합치거나 토큰 중간을 자르지 않는다. `Instantly`, `Microsoft 365` 같은 영문 제품명과 긴 한글 어절도 통째로 표시한다.
- 자막 양끝의 마침표·쉼표·물음표·느낌표·콜론·세미콜론·따옴표·괄호·말줄임표 등 문장부호는 모두 제거한다. 단, `fal.ai`, `Kling 2.6`처럼 제품명·도메인·버전 표기의 내부 점은 보존한다.
- 자막은 59px, x=540, y=940, 1줄, merge 비활성으로 고정하고 MP4에 굽는 동시에 SRT/ASS를 보존한다.
- 음성 -16 LUFS±0.5, 음성 true peak ≤-2dBTP, 음성-BGM ≥14LU, 음성 peak-SFX peak ≥8dB, 최종 -14 LUFS±0.5, 최종 true peak ≤-1.8dBTP를 모두 통과해야 한다.

## 검증·예약·교체

- 업로드 전에 `render_config.json`, `machine_validation.json`, `narration_alignment.json`, `02_exact_runtime_gate.json`, 최종 MP4를 같은 폴더에 둔다. `content_production_policy.validate_shorts_bundle_for_upload()` 통과 전에는 Aside가 Studio를 열지 않는다.
- 접촉 시트와 대표 구간 스크린샷으로 헤드카피, 원본 화면, 민수 원형 PIP, 자막, 워터마크를 직접 확인한다.
- 쇼츠와 카드뉴스 예약은 토요일·일요일을 포함해 매일 진행한다. 채널별 하루 최대 2개, 최소 5시간 간격이며 기본 시간은 Asia/Seoul 11:00와 20:00다. 예약 직전 공급자의 전체 예약 목록을 다시 읽어 중복 시각과 충돌을 검사한다.
- 잘못된 기존 예약을 교체할 때는 `새 영상 provider 저장/예약 확인 → 기존 예약 취소 또는 숨김 → 기존 항목 삭제 → CRM sent 1건` 순서를 지킨다. 새 URL/상태 확인 전 기존 것을 지우거나 CRM을 기록하지 않는다.
- 작업 중간 파일은 새 결과와 provider 상태를 확인한 뒤에만 정리한다. 진행 중 세션·자동화·누적 증거는 삭제하지 않는다.

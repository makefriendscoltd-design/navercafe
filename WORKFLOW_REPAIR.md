# 콘텐츠 자동화 복구 — 2026-09-06

정본은 `SHORTS_SPEC.md`와 `content_production_policy.py`다. 이 파일은 진행 기록이며 운영값의 별도 정본이 아니다.

## 승인된 순서

1. 입력·허용 변환·완료 계약의 충돌 해소.
2. NotebookLM 원응답과 실제 카페 본문·카드뉴스 입력·쇼츠 대본 연결 검증.
3. 실제 실패 응답 회귀 검증, 고정 true 검증 제거.
4. 검증된 변경을 main에 통합.
5. 공통 실행 코드 + 작업별 데이터, 대표 작업 별도 경로 검증.
6. 실제 Orca 자동화와 정본 동기화, 불확실한 게시 결과는 확인만 수행.
7. 대표 결과 대조와 차단 큐 원인별 복구.

## 확인한 원인 / 다시 쓰지 않을 방식

- NotebookLM 파일 존재만으로 게시 본문의 출처를 인정하지 않는다.
- 원문 실패를 팩트팩 재작성으로 자동 대체하지 않는다.
- 작업별 코드 복제, 고정 `meaningPreserved=True`, 조용한 생성 fallback을 새 경로에서 사용하지 않는다.
- 저장된 PASS만으로 파일 실재·현재 정책·실제 업로드를 추정하지 않는다.
- 게시 결과 불명확 시 재첨부/재게시하지 않는다.

## 통과 조건

- 기존 원응답의 형식만 변환한 결과는 통과, 다른 본문·원응답 교체·영상 교체는 차단.
- 최근 정상/실패 응답 및 현재 실제 산출물로 검증한다.
- 생성·업로드·공개 상태를 구별하며 현재 상태는 기존 채널 manifest/큐에서 확인한다.
- 공급자 성공 증거 없이 완료나 CRM 성공을 만들지 않는다.

## 시작 상태

- `shorts-v16-dclw-canary` HEAD b4147e2, main 5b68b30. 기존 미추적 build_outputs_index.py는 사용자 작업으로 보존.
- 카드뉴스 입력은 brain-sangchul/wiki/일하는-방식.md의 사용자 직접 발언에 따라 카페 NotebookLM 응답.
- 카페 큐 기록: 18 published / 18 blocked / 20 pending. 공급자 재조회 전 수치.
- dCLW 복구 MP4 존재, 재작성 경로이며 업로드 결과 불확실. 새 원문 보존 계약의 통과 사례로 사용하지 않는다.

## 진행

- 계약과 출처 검증 보강 중.

+- 실제 미발행 38건 검사: 20건 원문 불일치 / 17건 응답 누락 / 1건 레거시 manifest. 공급자 재조회 없이 published 항목은 변경하지 않음.
+- 원응답 형식만 재조립한 카페 후보 3건(0UFSZ_5OSIk, 5p-sq8v3OXw, dCLW6IQt06M) 통과. 나머지는 소제목 복구 또는 원응답 확보가 필요.
+- 실제 v16 0UFSZ 신규 1회 응답도 헤드카피 문장형·절대 성과 단정 문제. 동일 버전 반복 호출은 하지 않음.
+- 재시작 조건: v17에 검사기 문장형과 성과 단정 금지 명시 → 로컬 검사 → 지정 NotebookLM 저장/재조회 → 대표 원본 1회 → 대본/사실/원문 검증 후에만 TTS.
+- 관련 기존/회귀 검사 343개 + 사전계약 검사 14개 통과(첫 실행). 추가 변경 후 재검증 필요.
